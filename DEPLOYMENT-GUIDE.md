# BGAI 部署指南

## 架构概览

BGAI 应用采用了分布式微服务架构，整体部署架构如下：

```
                                    +-------------------+
                                    |    客户端应用     |
                                    +--------+---------+
                                             |
                                             v
+----------------------------------+  +------+---------+
|       中央服务器 (8.133.246.113)  |  |   应用容器     |
|----------------------------------|  |----------------|
| - Nacos (服务注册与配置中心)       |  | - Spring Boot |
| - MySQL (数据库)                  |  | - Web 服务     |
| - Redis (缓存与分布式锁)           |<-+ - API 层      |
| - Elasticsearch (全文检索)        |  | - 业务逻辑层   |
| - Seata (分布式事务协调器)         |  | - 监控客户端   |
| - RocketMQ (消息队列)             |  +----------------+
+----------------------------------+         ^
                                             |
                                    +--------+---------+
                                    |  监控工具（可选） |
                                    | - Prometheus    |
                                    | - Grafana       |
                                    +------------------+
```

## 部署模型说明

本项目采用了混合部署模式：
1. 核心基础服务（MySQL, Redis, Nacos, Elasticsearch, Seata, RocketMQ）部署在中央服务器 8.133.246.113
2. 应用服务通过 docker-compose 在本地或其他服务器上部署，连接到中央服务器上的基础服务
3. 监控服务（Prometheus, Grafana）可选择性部署在应用服务同一环境

这种部署模式的优势：
- 集中管理核心基础设施，避免重复部署
- 降低开发和测试环境的资源消耗
- 简化配置，所有应用连接同一套基础服务

## 环境参数

### 中央服务器信息

| 服务 | 主机 | 端口 | 凭据 |
|------|------|------|------|
| MySQL | 8.133.246.113 | 3306 | 用户: bgtech, 密码: Zly689258.. |
| Redis | 8.133.246.113 | 6379 | 密码: 5z25u2sk |
| Nacos | 8.133.246.113 | 8848 | 命名空间: d750d92e-152f-4055-a641-3bc9dda85a29 |
| Elasticsearch | 8.133.246.113 | 9200 | - |
| Seata | 8.133.246.113 | 8091 | - |
| RocketMQ | 8.133.246.113 | 9876 | - |

## docker-compose 使用说明

### 1. 启动应用

只需运行以下命令启动应用和监控工具：

```bash
docker-compose up -d
```

此命令将启动：
- bgai-app：主应用容器
- prometheus：可选的监控数据收集器
- grafana：可选的监控可视化平台

### 2. 仅启动应用（无监控）

```bash
docker-compose up -d bgai-app
```

### 3. 查看服务日志

```bash
# 查看应用日志
docker-compose logs -f bgai-app
```

### 4. 停止服务

```bash
docker-compose down
```

## 访问服务

- **应用服务**: http://localhost:8688
- **监控面板**: 
  - Prometheus: http://localhost:9090
  - Grafana: http://localhost:3000 (默认用户/密码: admin/admin)
- **中央服务**:
  - Nacos控制台: http://8.133.246.113:8848/nacos (用户名/密码: 请联系管理员)

## 环境变量说明

应用容器中的环境变量已配置为连接到中央服务器上的服务。如需修改，可编辑 `docker-compose.yml` 文件中的环境变量：

```yaml
environment:
  - REDIS_HOST=8.133.246.113
  - REDIS_PORT=6379
  - REDIS_PASSWORD=5z25u2sk
  - NACOS_HOST=8.133.246.113
  - NACOS_PORT=8848
  - NACOS_NAMESPACE=d750d92e-152f-4055-a641-3bc9dda85a29
  - MYSQL_HOST=8.133.246.113
  # ... 更多配置
```

## 故障排查

1. **无法连接到远程服务**：
   - 检查网络连接是否正常
   - 确认 8.133.246.113 服务器是否可访问
   - 验证服务器防火墙是否允许相应端口的访问

2. **应用启动失败**：
   - 查看日志：`docker-compose logs -f bgai-app`
   - 检查应用是否能正常连接到各远程服务
   - 确认环境变量配置是否正确

3. **监控工具问题**：
   - 确认 prometheus.yml 配置是否正确
   - 验证 Grafana 是否能成功连接到 Prometheus 数据源 