<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 引入Spring Boot默认的配置 -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    <springProperty scope="context" name="springAppName" source="spring.application.name"/>
    
    <!-- 添加启动时的状态监听器，确认配置加载正常 -->
    <statusListener class="ch.qos.logback.core.status.OnConsoleStatusListener" />

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [traceId:%X{traceId}] [userId:%X{userId}] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/${springAppName}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/${springAppName}.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [traceId:%X{traceId}] [userId:%X{userId}] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Logstash 输出 -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>8.133.246.113:5000</destination>
        <connectionStrategy>
            <roundRobin>
                <connectionTTL>5 minutes</connectionTTL>
            </roundRobin>
        </connectionStrategy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <protocol>beats</protocol>
            <customFields>{"app_name":"${springAppName}"}</customFields>
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <shortenedLoggerNameLength>36</shortenedLoggerNameLength>
            <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSS</timestampPattern>
            <writeVersionAsInteger>false</writeVersionAsInteger>
            <includeMdc>true</includeMdc>
            <includeContext>false</includeContext>
        </encoder>
        <keepAliveDuration>5 minutes</keepAliveDuration>
        <reconnectionDelay>30 second</reconnectionDelay>
        
        <!-- 添加条件，仅在非测试/非本地环境启用 -->
        <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
            <evaluator class="ch.qos.logback.classic.boolex.JaninoEventEvaluator">
                <expression>
                    String activeProfile = System.getProperty("spring.profiles.active", "");
                    return !(activeProfile.contains("test") || activeProfile.contains("local"));
                </expression>
            </evaluator>
            <OnMismatch>DENY</OnMismatch>
            <OnMatch>NEUTRAL</OnMatch>
        </filter>
    </appender>

    <!-- 异步处理 Logstash 输出 -->
    <appender name="ASYNC_LOGSTASH" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="LOGSTASH"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>true</includeCallerData>
        <neverBlock>true</neverBlock>
    </appender>

    <!-- 自定义日志级别 -->
    <logger name="com.bgpay.bgai" level="DEBUG"/>
    <logger name="com.bgpay.bgai.saga" level="DEBUG"/>
    
    <!-- Seata相关的日志 -->
    <logger name="io.seata" level="INFO"/>
    <!-- 特别禁用Seata警告 -->
    <logger name="io.seata.spring.boot.autoconfigure.provider.SpringBootConfigurationProvider" level="ERROR"/>

    <!-- Spring相关 -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.cloud.context.scope" level="WARN"/>
    <logger name="org.springframework.context.support" level="WARN"/>
    <logger name="org.springframework.beans.factory.support" level="WARN"/>
    <logger name="org.springframework.context.annotation" level="WARN"/>
    <logger name="org.springframework.context.support.PostProcessorRegistrationDelegate$BeanPostProcessorChecker" level="ERROR"/>
    
    <!-- MyBatis相关 -->
    <logger name="org.mybatis.spring.mapper" level="WARN"/>
    <logger name="com.baomidou.mybatisplus" level="WARN"/>

    <!-- Logstash日志级别控制 -->
    <logger name="net.logstash.logback.appender.LogstashTcpSocketAppender" level="ERROR" />
    <logger name="net.logstash.logback" level="ERROR" />

    <!-- 根日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <!-- 根据当前环境条件决定是否启用Logstash -->
        <springProfile name="!local,!test,!dev">
            <appender-ref ref="ASYNC_LOGSTASH"/>
        </springProfile>
    </root>
</configuration>